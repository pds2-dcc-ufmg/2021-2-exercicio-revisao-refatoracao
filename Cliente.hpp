#ifndef CLIENTE_H
#define CLIENTE_H

#include <string>

class Cliente{

	private:

		string Nome, Endereco, Cep, AlturaDosPais;
		
		// imprime na tela os dados de um cliente cadastrado
		void print()  
};

#endif
