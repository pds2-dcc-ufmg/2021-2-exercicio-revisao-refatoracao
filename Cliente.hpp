#ifndef CLIENTE_H
#define CLIENTE_H

#include <string>

class Cliente{

	public:

		string Nome, Endereco, Cep, AlturaDosPais;
		
		void print(); // imprime na tela os dados de um cliente cadastrado
};

#endif
